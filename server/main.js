import { Meteor } from 'meteor/meteor';
import '../imports/api/messagesserver.js';
import '../imports/api/privateKeys.js';
import '../imports/api/publicKeys.js';

Meteor.startup(() => {
  // code to run on server at startup
});
